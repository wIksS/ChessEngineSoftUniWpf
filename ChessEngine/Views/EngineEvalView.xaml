<Page x:Class="ChessEngine.Views.EngineEvalView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ChessEngine.Views"
      xmlns:controls="clr-namespace:ChessEngine.Controls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="EngineEvalView">

    <Grid>
        <controls:EvalSliderView CPScore="{Binding CP, Mode=OneWay}">
            <controls:EvalSliderView.Style>
                <Style TargetType="controls:EvalSliderView">
                    <Setter Property="Visibility" Value="Hidden"></Setter>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsEnabled}" Value="True">
                            <Setter Property="Visibility" Value="Visible"></Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </controls:EvalSliderView.Style>
        </controls:EvalSliderView>
    </Grid>
</Page>
